trigger E_EventTrigger on Event (after delete, after insert, after undelete, after update, before delete, before insert, before update) {
    
    E_EventTriggerHandler hander = new E_EventTriggerHandler(); 
    
    
}